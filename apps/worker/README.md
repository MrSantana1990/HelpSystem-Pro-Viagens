# Worker reservado

Ativação na fase de alertas: fila Redis, retries limitados, timeout, idempotência, dead-letter queue e heartbeat de progresso obrigatório. Nenhum loop vazio ou alerta é executado no bootstrap.
